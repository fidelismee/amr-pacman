# Git Commit & Push Workflow (with .gitignore hygiene)

This workflow updates `.gitignore` if necessary, then commits and pushes changes to the `main` branch.

---

## 1. Verify current branch
Check the current git branch.

```bash
git branch --show-current
